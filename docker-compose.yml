services:
  app:
    build: .
    container_name: pps-backend
    network_mode: host
    env_file: .env
    volumes:
      - ${UPDATES_HOST_DIR}:/app/deploy/pps_update
    restart: always

  # Uncomment saat Redis dibutuhkan:
  # redis:
  #   image: redis:7-alpine
  #   container_name: pps-redis
  #   restart: always
  #   volumes:
  #     - redis_data:/data

# volumes:
#   redis_data:
